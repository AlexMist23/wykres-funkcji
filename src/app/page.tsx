import Graph from "@/components/graph";

export default function LandingPage() {
  return (
    <main className="flex flex-1 mx-auto max-w-[2560px] w-full flex-col">
      <Graph />
    </main>
  );
}
